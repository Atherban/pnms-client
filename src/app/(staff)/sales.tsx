import AdminSidebar from "@/src/components/admin/BottomNavigation";
import React from "react";
import { View } from "react-native";

const sales = () => {
  return (
    <View>
      <AdminSidebar />
    </View>
  );
};

export default sales;
